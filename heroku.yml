setup:
  config:
    NODE_ENV: production
    SESSION_DRIVER: cookie
    ENV_SILENT: 'true'
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - node ace migration:run
run:
  web: node ace serve
